Отлично! 🚀 Тогда мы соберём полный сценарий собеседования под твою позицию — с готовыми ответами на все ключевые вопросы.

Я разделю его по блокам: вступление, вёрстка, Vue/Nuxt, TypeScript, архитектура, API, кроссбраузерность, сложные ситуации, завершение. Так ты сможешь читая или репетируя вслух, подготовиться полностью.

Вот структурированная версия:

⸻

💼 Полный сценарий собеседования — Frontend (Vue/Nuxt)

1️⃣ Вступление / “Расскажи о себе”

Вопрос: Расскажи о своём опыте и последних проектах.

Ответ:

В последние годы я в основном работал с Vue 2/3 и Nuxt 2/3, делал корпоративные сайты и SPA. Отвечал за адаптивную и pixel-perfect вёрстку по Figma, компонентную архитектуру и интеграцию с API.

Использовал Composition API, TypeScript, Pinia, Quasar, делал переиспользуемые компоненты, следил за качеством и читаемостью кода.

Также участвовал в проектировании структуры проектов, код-ревью и оптимизации производительности.

• Vue.js 2/3, Nuxt.js 2/3
• TypeScript, JavaScript (ES6+)
• Vuex, Pinia
• HTML5, CSS3, SASS/SCSS (БЭМ)
• Tailwind CSS, Element Plus, Vuetify, Quasar, PrimeVue, Nuxt UI, Shadcn Vue
• GSAP, PixiJS, Phaser.js
• Storybook, Axios

Тестирование:
• Jest, Vitest, Cypress, Playwright

Code Quality:
• ESLint, Prettier, Stylelint

Сборщики:
• Vite, Webpack, Gulp

Backend (базово):
• Node.js, NestJS

Инструменты:
• Git, GitHub, GitLab
• Jira, Figma, Notion

⸻

2️⃣ Работа с Figma / Pixel Perfect / Адаптив

Вопрос: Как ты работаешь с макетами из Figma?

Ответ:

Обычно я начинаю с анализа макета: смотрю сетку, отступы, цвета, типографику, повторяющиеся элементы.

Затем формирую базу UI-компонентов: кнопки, инпуты, карточки, модалки — чтобы сразу заложить переиспользуемость.

Настраиваю проект: переменные, шрифты, breakpoints, SCSS-структуру.

Дальше собираю интерфейс из компонентов, разделяю на виджеты и страницы, использую переменные и миксины для единого дизайна.

В конце проверяю pixel-perfect, адаптив и кроссбраузерность.

⸻

3️⃣ Кроссбраузерность

Вопрос: Как обеспечиваешь кроссбраузерность?

Ответ:

Сначала ориентируюсь на поддерживаемый список браузеров: Chrome, Firefox, Safari.

Проверяю поддержку CSS/JS через caniuse, избегаю нестабильных фич без полифиллов.

Тестирую в DevTools на разных разрешениях и DPI, отдельно проверяю мобильные версии через симулятор Xcode и реальные устройства.

При необходимости использую полифиллы и делаю graceful degradation.

⸻

4️⃣ BEM и организация SCSS

Вопрос: Как организуешь CSS/SCSS? Используешь ли BEM?

Ответ:

Структурирую стили по слоям: базовые стили, UI-компоненты, виджеты и страницы. Для каждого слоя отдельный SCSS-файл.

Использую BEM: блок — независимый компонент, элемент — его часть, модификатор — состояние. Это помогает избегать конфликтов и упрощает поддержку.

Пример:

```html
<div class="card card--featured">
  <h3 class="card__title"></h3>
</div>
```

Использую переменные, миксины и функции SCSS для единообразного дизайна.

⸻

5️⃣ Vue/Nuxt — архитектура компонентов

Вопрос: Как проектируешь компоненты во Vue, чтобы они были переиспользуемыми?

Ответ:

Стараюсь придерживаться принципа единой ответственности. Если компонент становится слишком большим и появляются десятки пропсов — делю его на сущности.

Логику выношу в composables, UI держу максимально простым.

Использую props для входных данных, emits для событий, slots для гибкости.

Компоненты должны быть переиспользуемыми и независимыми от конкретного бизнес-контекста.

Для сложных частей применяю строгую типизацию через TypeScript.

⸻

6️⃣ TypeScript

Вопрос: Как используешь TS в проектах?

Ответ:

В первую очередь типизирую данные с API через интерфейсы, чтобы предотвратить ошибки при работе с данными.

Во вторую очередь типизирую роуты и навигацию через интерфейсы и enum, чтобы централизованно управлять путями.

Также типизирую props, emits и локальные состояния компонентов.

Работаю в strict mode, избегаю any, чтобы TS реально помогал, а не был формальностью.

TS снижает runtime-ошибки, упрощает рефакторинг и делает код предсказуемым.

⸻

7️⃣ Работа с API

Вопрос: Как организуешь работу с API?

Ответ:

Выношу все запросы в отдельный слой (api или services), группируя их по доменам: profile, auth, orders.

Для каждого модуля методы: getUser, changeEmail, resendCode, saveUser.

Запросы через async/await, базовая обработка ошибок внутри сервиса.

В компонентах использую try/catch, управляю состояниями isLoading/error/success, показываю уведомления пользователю.

Типизирую ответы API, чтобы компоненты не работали с сырыми данными.

⸻

8️⃣ Сложные ситуации / баги

Вопрос: Расскажи про сложный баг, который пришлось решать.

Ответ:

У нас был онлайн-игровой модуль, который зависал при загрузке на одном сайте, хотя на другом работал.

Проверил билд и подменил на рабочую версию — игра запускалась. Это показало, что проблема в билде.

Скачал неработающий билд, закоммитил в Git и сравнил с рабочим билдом через систему контроля версий.

Выяснилось, что различия были в HTTP-заголовках. После исправления заголовков модуль заработал корректно.

Этот опыт показал важность системного подхода: локализовать проблему, сравнивать версии, анализировать данные, а не только интерфейс.

⸻

9️⃣ Завершение / вопросы к рекрутеру

Совет: подготовь 2–3 вопроса, чтобы показать вовлечённость:
• Про процесс код-ревью
• Про дизайн-систему и компонентную библиотеку
• Про командные стандарты по фронтенду

⸻

Если хочешь, я могу сделать короткий “шпаргалочный вариант” на 1 страницу, где каждая тема — 1–2 предложения. Его удобно читать прямо перед собеседованием.

Хочешь, чтобы я так сделал?

### Объясни разницу между var, let и const. Когда что используешь?

var: имеет функциональную (или глобальную) область видимости, допускает повторное объявление, поднятие (hoisting) работает до объявления, но не для инициализации. Сейчас практически не используется в современном JS.

let: блочная область видимости, нельзя объявить повторно в одной области видимости, поддерживает hoisting, но нельзя использовать до объявления. Используется для переменных, значение которых будет меняться.

const: также блочная область видимости, нельзя переназначить саму переменную, но если это объект или массив — его содержимое можно изменять. Используется для неизменяемых ссылок на данные.

### Вопрос: Что такое замыкание (closure) и где его можно использовать?

Замыкание — это функция, которая имеет доступ к переменным своей внешней функции даже после того, как внешняя функция завершила выполнение.

Проще говоря, внутреняя функция «помнит» область видимости, в которой была создана.

Используется для:
• Инкапсуляции данных (например, приватные переменные)
• Создания фабрик функций
• Частичного применения (currying)
• Ленивая инициализация или кеширование значений

```ts
function counter() {
  let count = 0;

  return function () {
    count+++;
    return count;
  };
}

const increment = counter();
console.log(increment()); // 1
console.log(increment()); // 2
```

### Вопрос: Какие бывают функции в JavaScript и чем они отличаются?

Коротко: отличаются синтаксисом, контекстом `this`, возможностью вызова до объявления (hoisting) и тем, есть ли у функции собственный `this`.

Основные виды:

- **Function Declaration** — всплывает (hoisting), можно вызывать до объявления, имеет собственный `this`.

  ```ts
  function sum(a: number, b: number) {
    return a + b;
  }
  ```

- **Function Expression** — не всплывает как готовая функция (только переменная), удобна для передачи как значения.

  ```ts
  const sum = function (a: number, b: number) {
    return a + b;
  };
  ```

- **Arrow Function** — нет собственного `this`, `arguments`, `super`; `this` берется из внешней области (lexical `this`).

  ```ts
  const sum = (a: number, b: number) => a + b;
  ```

- **Method** — функция как свойство объекта; `this` зависит от способа вызова.

  ```ts
  const obj = {
    value: 10,
    getValue() {
      return this.value;
    },
  };
  ```

- **Constructor Function / Class Method** — вызывается через `new`, создает экземпляр.

  ```ts
  function User(name: string) {
    this.name = name;
  }
  const user = new User('Ann');
  ```

- **IIFE** — сразу выполняется, удобна для изоляции области видимости.
  ```ts
  (function () {
    const secret = 42;
  })();
  ```

Практика: для методов и обработчиков событий чаще подойдут обычные функции (контроль `this`), для коротких коллбеков — стрелочные.

### Объясни, как работает this в JavaScript. Какие бывают контексты?

this — это объект, к которому привязан текущий контекст выполнения функции. Его значение зависит от способа вызова функции: 1. В глобальной области (нестрогий режим) — this ссылается на глобальный объект (window в браузере). 2. В строгом режиме — this в глобальной функции undefined. 3. Методы объекта — this ссылается на объект, которому принадлежит метод. 4. Конструкторы / классы — this ссылается на создаваемый экземпляр. 5. Явное связывание — call, apply, bind позволяют задать конкретный объект. 6. Стрелочные функции — не имеют собственного this, берут его из внешней области видимости.

```ts
const obj = {
  name: 'Alice',
  sayName: function () {
    console.log(this.name);
  },
};

obj.sayName(); // Alice

const fn = obj.sayName;
fn(); // undefined (strict mode)
```

### Что такое прототипы в JavaScript и как работает наследование через них?

В JavaScript объекты могут наследовать свойства и методы друг от друга через прототипную цепочку.

Каждый объект имеет скрытое свойство [[Prototype]] (доступно через **proto** или Object.getPrototypeOf(obj)), которое указывает на объект-прототип.

Когда мы обращаемся к свойству объекта, JS сначала ищет его у самого объекта, а если не находит — идёт по цепочке прототипов.

Это и есть прототипное наследование.

```ts
const person = {
  greet() {
    console.log('Hello');
  },
};

const user = Object.create(person);
user.name = 'Alice';

user.greet(); // Hello
```

✅ Здесь user наследует метод greet от person через прототип.
Прототипы позволяют экономить память: методы не дублируются в каждом объекте, а используются через цепочку наследования.

### Чем отличаются callback, Promise и async/await? Когда что используешь?

Callback — это функция, которую мы передаём в другую функцию и которая вызывается после завершения какого-либо действия. Проблема: часто приводит к «callback hell» при вложенных вызовах.

пример:

```ts
setTimeout(() => {
  console.log('Hello after 1 second');
}, 1000);
```

Promise — объект, который представляет результат асинхронной операции. Позволяет использовать .then() и .catch() для обработки успешного и ошибочного результата, избегая глубоких вложенностей.

```ts
fetch('/api/data')
  .then((res) => res.json())
  .then((data) => console.log(data))
  .catch((err) => console.error(err));
```

async/await — синтаксический сахар над Promise. Позволяет писать асинхронный код как синхронный, с try/catch для обработки ошибок.

```ts
async function getData() {
  try {
    const res = await fetch('/api/data');
    const data = await res.json();
    console.log(data);
  } catch (err) {
    console.error(err);
  }
}
```

🔹 Когда что использовать
• Callback — редко, только для старого кода или простых событий.
• Promise — когда нужна цепочка асинхронных действий.
• async/await — современный и удобный способ писать асинхронный код, особенно в больших проектах.

### Чем отличается == от === в JavaScript?

== — нестрогое сравнение (loose equality). Сначала пытается привести типы к общему, а потом сравнивает значения.
=== — строгое сравнение (strict equality). Сравнивает и тип, и значение, без приведения типов.

```ts
1 == '1'; // true, потому что '1' приводится к числу
1 === '1'; // false, разные типы
```

всегда используем ===, чтобы избежать неожиданных багов с приведением типов.

### Объясни, как работает Event Loop в JavaScript. Что такое call stack, task queue и microtask queue?

JavaScript однопоточный, но асинхронный код выполняется через event loop.

Call stack — стек вызовов функций, выполняющихся синхронно.

Task queue (macrotasks) — очередь для отложенных задач, например setTimeout, setInterval, сетевые события.

Microtask queue — очередь для Promise, process.nextTick (Node), MutationObserver. Microtasks выполняются сразу после текущего стека, до следующей macrotask.

Event Loop работает так: 1. Выполняется стек вызовов (синхронный код). 2. Выполняются все microtasks. 3. Берется следующая macrotask из task queue и повторяем цикл.

```ts
console.log('1');

setTimeout(() => console.log('2'), 0);

Promise.resolve().then(() => console.log('3'));

console.log('4');

// Вывод: 1, 4, 3, 2
```

✅ Здесь видно, что microtask (Promise) выполнится раньше setTimeout, даже с delay=0.

### Что такое hoisting в JavaScript? Как он работает с var, let и const?

Hoisting — это поведение JavaScript, при котором объявления переменных и функций «поднимаются» в начало своей области видимости во время компиляции.
• Для var: объявление поднимается, но инициализация остаётся на месте. Поэтому переменные существуют до строки кода, но равны undefined.
• Для let и const: объявление тоже поднимается, но они находятся в temporal dead zone (TDZ) до строки объявления, и использовать их до объявления нельзя.
• Для функций: объявления функций полностью поднимаются, поэтому их можно вызывать до того, как они определены в коде.

```ts
console.log(a); // undefined
var a = 5;

console.log(b); // ReferenceError
let b = 10;

console.log(c()); // "Hello"
function c() {
  return 'Hello';
}
```

Hoisting — это только для объявлений, а не для присвоений.

• let и const защищают от ошибок, которые могли бы возникнуть с var.

### Объясни разницу между spread и rest, и как используется destructuring.

Spread (...) — оператор, который разворачивает массив или объект в местах, где ожидаются элементы или свойства. Используется для создания новых копий, объединения массивов/объектов и передачи аргументов функции.

Rest (...) — оператор, который собирает остаток элементов или свойств в массив или объект. Используется для получения неопределённого числа аргументов или оставшихся свойств объекта.

Destructuring — синтаксис для деструктуризации массивов или объектов, позволяет извлекать значения в отдельные переменные.

Spread для массива:

```ts
const arr1 = [1, 2];
const arr2 = [...arr1, 3]; // [1, 2, 3]
```

Rest в функции:

```ts
function sum(...nums) {
  return nums.reduce((a, b) => a + b, 0);
}
sum(1, 2, 3); // 6
```

Destructuring объекта:

```ts
const user = { name: 'Alice', age: 25 };
const { name, age } = user;
console.log(name, age); // Alice 25
```

Destructuring массива:

```ts
const numbers = [10, 20, 30];
const [first, ...rest] = numbers;
console.log(first); // 10
console.log(rest); // [20, 30]
```

всегда называй, что spread — для разворачивания, rest — для сбора, а destructuring — для деструктуризации объектов и массивов.

### В чём разница между forEach, map, filter и reduce? Когда что используешь?

forEach — перебирает элементы массива, выполняет функцию для каждого элемента. Не возвращает новый массив, используется для побочных эффектов.

map — перебирает элементы массива и возвращает новый массив, где каждый элемент трансформирован функцией.

filter — перебирает элементы и возвращает новый массив, содержащий только элементы, которые удовлетворяют условию.

reduce — аккумулирует значения массива в одно значение (число, объект, массив и т.д.) через callback-функцию с аккумулятором.

```ts
const numbers = [1, 2, 3, 4];

// forEach
numbers.forEach(n => console.log(n)); // 1 2 3 4

// map
const doubled = numbers.map(n => n \* 2); // [2, 4, 6, 8]

// filter
const even = numbers.filter(n => n % 2 === 0); // [2, 4]

// reduce
const sum = numbers.reduce((acc, n) => acc + n, 0); // 10
```

упомяни, что map и filter возвращают новый массив, reduce — универсальный метод для агрегирования данных, а forEach — для побочных эффектов.

### Объясни жизненный цикл компонента Vue 3 (Composition API или Options API) и где что обычно используешь.

В Options API ключевые хуки:
• created — компонент создан, данные и реактивные свойства уже доступны, но DOM ещё не смонтирован. Обычно используем для инициализации данных, API-запросов.
• mounted — компонент смонтирован в DOM. Используется для работы с DOM, сторонними библиотеками, плагинами.
• updated — компонент обновлён, реактивные данные изменились.
• beforeUnmount / unmounted — перед удалением / после удаления компонента, для очистки таймеров, подписок, событий.

В Composition API используются onMounted, onBeforeUnmount и другие lifecycle хуки, импортируемые из vue:

```ts
import { onMounted, onBeforeUnmount, ref } from 'vue';

export default {
  setup() {
    const count = ref(0);

    onMounted(() => {
      console.log('Компонент смонтирован');
    });

    onBeforeUnmount(() => {
      console.log('Компонент будет уничтожен');
    });

    return { count };
  },
};
```

жизненный цикл   nuxt3

В Nuxt 3 “жизненный цикл” = комбинация SSR/CSR этапов + хуков Vue 3 + хуков Nuxt (плагины/роутер/приложение).

1) Большая картинка: что происходит при заходе на страницу

A) Первый заход (SSR)
	1.	Server: создаётся Nuxt app, выполняются плагины (server-side).
	2.	Server: рендерится страница в HTML (в этот момент onMounted НЕ выполнится).
	3.	Client: браузер получает HTML.
	4.	Client: происходит hydration (подключение JS к уже готовому HTML).
	5.	Client: запускаются client-плагины, затем onMounted.

B) Переход по роутам внутри приложения (CSR)
	•	SSR уже нет, всё работает как SPA:
	•	выполняются роут-хуки
	•	обновляются компоненты
	•	работают обычные Vue lifecycle hooks

⸻

2) Что важно помнить на собеседовании

Vue lifecycle (в компоненте)
	•	setup() — выполняется и на сервере, и на клиенте
	•	onBeforeMount, onMounted — только клиент
	•	onServerPrefetch — только сервер (полезно для SSR данных)
	•	onUpdated, onUnmounted — клиент (и при CSR переходах)

Nuxt data lifecycle (получение данных)
	•	useAsyncData() / useFetch():
	•	на первом заходе выполняются на сервере (если SSR включён) и результат сериализуется в payload
	•	на клиентских переходах выполняются на клиенте
	•	Если надо “только клиент”:
	•	useAsyncData({ server: false })
	•	или if (process.client) ...
	•	или обернуть кусок в <ClientOnly>

⸻

3) Полезные хуки Nuxt (коротко)

Роутер
	•	Middleware:
	•	defineNuxtRouteMiddleware((to, from) => ...)
	•	бывает глобальный /middleware/*.global.ts
	•	или именованный и подключается через definePageMeta({ middleware: ... })

App hooks
	•	nuxtApp.hook('page:start', ...)
	•	nuxtApp.hook('page:finish', ...)
	•	nuxtApp.hook('app:mounted', ...) (когда приложение смонтировано на клиенте)

Плагины
	•	defineNuxtPlugin(() => { ... })
	•	можно ограничить окружение файлами:
	•	plugin.client.ts (только клиент)
	•	plugin.server.ts (только сервер)

⸻

4) Мини-шпаргалка “где что выполняется”

Server + Client:
	•	setup()
	•	useAsyncData() (по умолчанию)
	•	middleware (на SSR и при CSR переходах)

Только Server:
	•	onServerPrefetch
	•	plugin.server.ts

Только Client:
	•	onMounted, window/document, DOM
	•	plugin.client.ts
	•	<ClientOnly>

⸻

5) Формулировка для собеса (готовая)

“В Nuxt 3 первый рендер обычно SSR: setup и useAsyncData выполняются на сервере, HTML уходит в браузер, затем на клиенте происходит hydration и только после этого срабатывает onMounted. При навигации внутри приложения всё идёт как SPA: данные/хуки выполняются на клиенте. Для логики, завязанной на DOM, используем onMounted/ClientOnly, а для SSR данных — useAsyncData или onServerPrefetch.”


### В чём разница между Composition API и Options API? Когда что используешь?

Options API — классический способ написания компонентов. Всё разделено на опции: data, methods, computed, watch, mounted и т.д. Внутри компонентов есть глобальный контекст (this). Удобно для небольших компонентов и новичков.

Composition API — современный подход через setup(). Позволяет импортировать только нужные реактивные функции (ref, reactive, computed, watch) и хуки жизненного цикла (onMounted, onBeforeUnmount). Нет глобального контекста this, всё возвращается из setup().

Плюсы Composition API:
• Легче разделять и повторно использовать логику через composables
• Удобно для больших компонентов с множеством реактивных данных

Используемый подход: в Vue 3 часто комбинируем Options API + composables, чтобы сохранять читаемость компонентов, но при этом использовать повторно логику.

```ts
// useCounter.js
import { ref } from 'vue';
export function useCounter() {
  const count = ref(0);
  const increment = () => count.value++;
  return { count, increment };
}

// В компоненте
import { useCounter } from './useCounter';
export default {
  setup() {
    const { count, increment } = useCounter();
    return { count, increment };
  },
};
```

### Как ты работаешь с props, emits и slots во Vue 3? Когда что используешь?

Props — входные данные для компонента. Используются, когда компонент переиспользуемый или дочерний, чтобы передавать ему данные от родителя.

Emits — событие, которое компонент «испускает» наружу, чтобы родитель мог на него реагировать. Используется для обратной связи от дочернего к родительскому компоненту.

Slots — место для динамического контента внутри компонента. Особенно удобно для модалок, карточек, оберток, где нужно отображать разные элементы в одном и том же компоненте.

<!-- Родитель -->

```ts
<CustomModal @close="handleClose">
<template #default>
<p>Динамический контент</p>
</template>
</CustomModal>

<!-- CustomModal.vue -->
<template>
  <div class="modal">
    <slot></slot>
    <button @click="$emit('close')">Закрыть</button>
  </div>
</template>
<script>
export default {
  props: { title: String },
  emits: ['close'],
};
</script>
```

всегда упоминай переиспользуемость компонентов и чистоту архитектуры. Это показывает системное мышление.

### Как ты организуешь проект на Nuxt 2/3? Где хранятся страницы, компоненты, composables, и как интегрируешь API?

В Nuxt страницы хранятся в папке pages. Их имена соответствуют роутам приложения — это обеспечивает автоматическое роутинг.

Компоненты располагаются в папке components и структурированы по назначению:
• components/ui — базовые UI-компоненты (кнопки, инпуты, селекты, date pickers)
• components/widgets — виджеты, собираемые на базе UI, переиспользуемые на страницах
• components/modals — все модальные окна приложения
• components/layout — обертки для страниц (например, default layout, 404)
• components/helpers — вспомогательные компоненты, например, обертки для fetch с загрузкой и слотами для контента

Composables хранятся в папке composables. Здесь я размещаю переиспользуемую логику, методы API, реактивные состояния и кешируемые переменные.

API интегрируется через composables или отдельный слой api/services. Компоненты не работают напрямую с API, используют composables для запроса данных, обработки ошибок и состояний загрузки.

💡 Совет для интервью:
Если спросят “почему так структурируешь?”, можно добавить:

Такая структура помогает разделять ответственность, делать компоненты и логику переиспользуемыми, легко поддерживать и масштабировать проект.

### Как ты используешь asyncData и fetch в Nuxt? Чем они отличаются и когда применяешь?

asyncData — используется для получения данных до рендеринга страницы. Работает только в компонентах страниц (pages). Позволяет получать данные как на сервере (SSR), так и на клиенте при навигации, и возвращает объект, который мержится с данными компонента.

fetch — используется для загрузки данных после инициализации компонента, может применяться в страницах и компонентах. В Nuxt 3 работает через Composition API. Позволяет изменять реактивные состояния и не возвращает объект напрямую.

    •	asyncData — когда нужно, чтобы данные были готовы до рендера страницы, например для SEO и SSR.
    •	fetch — когда нужно динамически обновлять состояние или использовать данные внутри компонентов.

asyncData (страницы)

```vue
<script setup>
const { params } = useRoute(); // доступ к роуту
const { data: posts } = await useAsyncData('posts', () => $fetch('/api/posts'));
</script>

<template>
  <div>
    <h1>Posts</h1>
    <ul>
      <li v-for="post in posts" :key="post.id">{{ post.title }}</li>
    </ul>
  </div>
</template>
```

useAsyncData заменяет старый asyncData.
• Можно использовать в <script setup> напрямую.
• Данные доступны до рендера страницы (SSR/CSR).

fetch (страницы или компоненты)

```vue
<script setup>
import { ref } from 'vue';

const posts = ref([]);
const isLoading = ref(true);
const error = ref(null);

async function loadPosts() {
  try {
    posts.value = await $fetch('/api/posts');
  } catch (err) {
    error.value = err;
  } finally {
    isLoading.value = false;
  }
}

await loadPosts(); // можно вызвать при инициализации
</script>

<template>
  <div v-if="isLoading">Loading...</div>
  <div v-else-if="error">{{ error.message }}</div>
  <ul v-else>
    <li v-for="post in posts" :key="post.id">{{ post.title }}</li>
  </ul>
</template>
```

В Composition API для динамических данных часто используют ref + await $fetch.
fetch позволяет управлять состояниями загрузки и ошибок внутри компонента.

useAsyncData — для данных, которые должны быть готовы до рендера страницы (SSR, SEO).
fetch / $fetch — для динамических запросов после инициализации компонента или для компонентов, не являющихся страницами.

### как ты реализуешь динамические маршруты и вложенные страницы в Nuxt 3? Когда это используется?

В Nuxt 3 страницы создаются в папке pages, и структура папок отражает маршруты.

Динамические маршруты создаются с помощью квадратных скобок []. Например:
• pages/posts/[id]/index.vue → путь /posts/4 для id = 4
• Внутри компонента можно получить параметр через useRoute() или useAsyncData():

const { params } = useRoute();
console.log(params.id); // 4

Вложенные страницы создаются через вложенные директории. Например:
• pages/posts/index.vue → /posts
• pages/posts/[id]/index.vue → /posts/4

Nuxt автоматически формирует маршруты на основе структуры папок, что упрощает организацию страниц и вложенных маршрутов.

### Как ты используешь Pinia в проектах на Nuxt 3? Когда и зачем применяешь?

Pinia — это современное глобальное хранилище состояния для Vue 3 / Nuxt 3. Позволяет централизованно хранить данные, методы работы с ними и реактивные состояния.

Используется для:
• Хранения пользовательских данных (user, token, permissions)
• Общего состояния приложения (cart, settings, theme)
• Кешируемых переменных, которые должны быть доступны из любого компонента

Принцип работы похож на composables: мы создаём store, экспортируем его и используем в компонентах через Composition API.

Пример Pinia store:

```ts
// stores/user.js
import { defineStore } from 'pinia';

export const useUserStore = defineStore('user', {
  state: () => ({
    user: null,
    token: '',
  }),
  actions: {
    setUser(userData) {
      this.user = userData;
    },
    logout() {
      this.user = null;
      this.token = '';
    },
  },
});
```

Использование в компоненте:

```ts
<script setup>
import { useUserStore } from '@/stores/user';
const userStore = useUserStore();

userStore.setUser({ name: 'Alice' });
console.log(userStore.user);
</script>
```

• Подчеркни, что Pinia позволяет разделять логику состояния по модулям, легко тестировать и переиспользовать.
• В Nuxt 3 store подключается автоматически через папку stores, что упрощает интеграцию.

### Для чего используется TypeScript? Какие преимущества он даёт по сравнению с чистым JavaScript?

TypeScript (TS) — это надстройка над JavaScript, которая добавляет строгую типизацию, статическую проверку типов и современные возможности языка.

Преимущества:
• Позволяет ловить ошибки типов на этапе компиляции, а не в runtime
• Улучшает читаемость и поддержку кода
• Облегчает работу в команде: другие разработчики сразу видят структуру данных через interface и типы
• Удобно для автодополнения и рефакторинга в IDE
• Сочетается с Vue 3 (Composition API), Pinia и Nuxt 3

```ts
interface User {
  id: number;
  name: string;
}

function greet(user: User) {
  console.log(`Hello, ${user.name}`);
}

greet({ id: 1, name: 'Alice' }); // OK
greet({ id: 'wrong', name: 'Bob' }); // Ошибка на этапе компиляции
```

Показывай, что TS не просто для интерфейсов, а реально помогает снизить баги и улучшить поддержку проекта.

### В чём разница между interface и type в TypeScript? Когда что использовать?

```ts
interface
	•	Используется для описания структуры объекта.
	•	Может расширяться (extends) другими интерфейсами.
	•	Обычно используют для объектов и классов.

interface User {
  id: number;
  name: string;
}

interface Admin extends User {
  role: string;
}

const admin: Admin = { id: 1, name: 'Alice', role: 'admin' };
```

type
• Используется для создания алиасов типов: объектов, примитивов, объединений (union) или пересечений (intersection).
• Более универсальный, чем interface.

```ts
type ID = number | string;
type User = { id: ID; name: string };

const user: User = { id: 1, name: 'Bob' };
```

Главное отличие:
• interface расширяемый, идеально для описания объектов/классов.
• type — не расширяемый интерфейс напрямую, но можно комбинировать через & и |, универсален для любых типов.

Использую interface для объектов и классов, а type — для алиасов, объединений и пересечений.

### Что такое generics в TypeScript и зачем их использовать?

Generics — это способ создавать функции, классы или интерфейсы, которые могут работать с разными типами данных, сохраняя типизацию.

Используются для:
• Универсальных функций и компонентов
• Обобщённых классов и интерфейсов
• Обработки данных в Promise, API и коллекциях

Функция с generic:

```ts
function wrap<T>(value: T): { data: T } {
  return { data: value };
}

const wrap2 = <T>(value: T): { data: T } => {
  return { data: value }
}

const num = wrap(5);          // { data: number }
const str = wrap('hello');    // { data: string }

Promise с generic:
async function fetchData<T>(url: string): Promise<T> {
  const res = await fetch(url);
  return res.json() as Promise<T>;
}

interface User {
  id: number;
  name: string;
}

const user = await fetchData<User>('/api/user');
console.log(user.name);
```

💡 Совет для интервью:
Даже если не использовал generics на практике, покажи понимание, что они обобщают типы и делают код более безопасным и переиспользуемым.

### Как ты используешь TypeScript для типизации props, emits и данных API в Vue 3 / Nuxt 3?

1. Props
   • Типизируем входные данные для компонентов через defineProps или Options API.

```ts
<script setup lang="ts">
interface User {
  id: number;
  name: string;
}

const props = defineProps<{ user: User }>();
console.log(props.user.name);
</script>
```

2. Emits
   • Типизируем события, чтобы родитель знал, какие данные получает.

```ts
const emit = defineEmits<{(e: 'update', value: number): void }>();
emit('update', 5);

если несколько
const emit = defineEmits<{
  (e: 'update', value: number): void,
  (e: 'change', value: number): void,
}>();
```

3. Данные API
   • Типизируем данные, получаемые с сервера, через интерфейсы и generics.

```ts
interface Post {
  id: number;
  title: string;
}

const { data: posts } = await useAsyncData<Post[]>('posts', () =>
  $fetch('/api/posts'),
);
console.log(posts[0].title);
```

Покажи, что TypeScript помогает ловить ошибки типов ещё на этапе компиляции, делать код безопасным и понятным, особенно при работе с API и компонентами.

### Как ты используешь TypeScript для типизации состояния и методов Pinia, а также для работы с API в Nuxt 3?

TypeScript позволяет строго типизировать состояние, методы и данные, получаемые с API, чтобы избежать ошибок на этапе компиляции и сделать код более поддерживаемым.

Пример Pinia Store с типами:

```ts
import { defineStore } from 'pinia';

interface User {
  id: number;
  name: string;
}

export const useUserStore = defineStore('user', {
  state: () => ({
    user: null as User | null,
    token: '' as string,
  }),
  actions: {
    setUser(userData: User) {
      this.user = userData;
    },
    logout() {
      this.user = null;
      this.token = '';
    },
  },
});
```

Типизация API запросов с generics:

```ts
interface Post {
  id: number;
  title: string;
}

async function fetchPosts(): Promise<Post[]> {
  return await $fetch('/api/posts');
}
```

```ts
// Используем в Pinia
export const usePostsStore = defineStore('posts', {
  state: () => ({ posts: [] as Post[] }),
  actions: {
    async loadPosts() {
      this.posts = await fetchPosts();
    },
  },
});
```

# ООП

### Что такое ООП и какие его основные принципы?

ООП (Объектно-ориентированное программирование) — это парадигма, где код строится вокруг объектов, которые содержат данные (состояние) и методы (поведение).

Основные принципы:

1. Инкапсуляция — скрытие внутреннего состояния объекта и предоставление доступа через методы.

```ts
class User {
  private password: string;
  constructor(
    public name: string,
    password: string,
  ) {
    this.password = password;
  }
  checkPassword(pass: string) {
    return this.password === pass;
  }
}
```

2. Наследование — создание нового класса на основе существующего.

```ts
class Admin extends User {
  role: string = 'admin';
}
```

3. Полиморфизм — способность объектов разного типа использовать один и тот же интерфейс.

```ts
class Guest extends User {
  checkPassword() {
    return false; // переопределяем метод
  }
}
```

4. Абстракция — выделение только важных характеристик объекта, скрытие деталей реализации.

### Как в TypeScript реализуются интерфейсы, абстрактные классы и модификаторы доступа? Когда что используешь?

1️⃣ Интерфейсы (interface)

    •	Определяют структуру объекта или класса (какие свойства и методы обязательны).
    •	Используются для контракта, чтобы класс или объект точно соответствовал определённой структуре.

```ts
interface IUser {
  id: number;
  name: string;
  login(): void;
}

class User implements IUser {
  constructor(
    public id: number,
    public name: string,
  ) {}
  login() {
    console.log(`${this.name} logged in`);
  }
}
```

2️⃣ Абстрактные классы (abstract class)

    •	Нельзя создать экземпляр напрямую.
    •	Используются для базового класса, который задаёт обязательные методы для наследников.

```ts
abstract class Person {
  constructor(public name: string) {}
  abstract greet(): void; // обязателен для наследников
}

class Admin extends Person {
  greet() {
    console.log(`Hello, ${this.name}`);
  }
}
```

3️⃣ Модификаторы доступа
• public — доступно везде (по умолчанию).
• private — доступно только внутри класса.
• protected — доступно внутри класса и наследников.

```ts
class User {
  private password: string;
  protected role: string;
  public name: string;

  constructor(name: string, password: string, role: string) {
    this.name = name;
    this.password = password;
    this.role = role;
  }
}
```

Используй интерфейсы для контракта, абстрактные классы для общей логики с обязательными методами, а модификаторы доступа — чтобы контролировать инкапсуляцию.

### Как в Vue/TS можно применять наследование, полиморфизм и композицию? Когда что использовать?

1️⃣ Наследование
• Используется, когда нужно переиспользовать общую логику между компонентами или классами.
• В Vue это чаще реализуется через базовые классы для сервисов, моделей или composables.

```ts
abstract class BaseService {
  abstract fetchData(): Promise<any>;
}

class UserService extends BaseService {
  async fetchData() {
    return await $fetch('/api/users');
  }
}
```

2️⃣ Полиморфизм
• Позволяет разным объектам использовать один и тот же интерфейс или метод по-разному.
• В Vue/TS можно применять через интерфейсы или абстрактные классы.

```ts
interface Logger {
  log(message: string): void;
}

class ConsoleLogger implements Logger {
  log(message: string) {
    console.log(message);
  }
}

class FileLogger implements Logger {
  log(message: string) {
    // логика записи в файл
  }
}
```

3️⃣ Композиция
• Предпочтительный подход во Vue 3 — разделяем логику через composables, вместо классического наследования.
• Позволяет создавать переиспользуемые куски логики без жесткой иерархии.

```ts
// useCounter.ts
import { ref } from 'vue';

export function useCounter() {
  const count = ref(0);
  const increment = () => count.value++;
  return { count, increment };
}

// В компоненте
const { count, increment } = useCounter();
```

На фронтенде чаще применяют композицию (composables), наследование и полиморфизм используют для сервисов, API и моделей, а не для компонентов напрямую.

1️⃣ SOLID — принципы ООП

Вопрос: Что такое SOLID и как применяешь принципы на фронтенде?

SOLID — набор принципов, который помогает писать поддерживаемый, расширяемый и читаемый код. 1. S — Single Responsibility Principle (SRP)
• Класс или компонент должен иметь одну ответственность.
• Во Vue: один компонент отвечает за один UI-блок или виджет.

    2.	O — Open/Closed Principle (OCP)
    •	Класс/функция открыт для расширения, но закрыт для изменения.
    •	Во Vue/Nuxt: используем composables и слоты, чтобы расширять функционал без изменения исходного кода.

    3.	L — Liskov Substitution Principle (LSP)
    •	Наследуемые классы должны работать вместо базовых без ошибок.
    •	Во Vue/TS: сервисы или store можно заменить на другой класс с тем же интерфейсом.

    4.	I — Interface Segregation Principle (ISP)
    •	Интерфейсы должны быть узкими и специфичными, не перегружать методы.
    •	Во TS: создаём маленькие интерфейсы для props, API, composables.

    5.	D — Dependency Inversion Principle (DIP)
    •	Зависимости должны быть через абстракции, а не конкретные реализации.
    •	Во Vue/Nuxt: store или сервисы через интерфейсы, DI в Nuxt 3 (provide/inject).

S — Single Responsibility Principle (SRP)

Суть:
Каждый компонент, класс или модуль должен отвечать за одну задачу.

Пример на Vue/Nuxt:

```vue
// Компонент Button.vue — только отображение кнопки
<template>
  <button :class="type" @click="click">{{ label }}</button>
</template>

<script setup lang="ts">
defineProps<{ label: string; type?: string }>();
defineEmits<['click']>();
</script>
```

```ts
// Composable useUser.ts — только логика работы с пользователем
import { ref } from 'vue';

const user = ref<User | null>(null);

export function useUser() {
  const setUser = (data: User) => (user.value = data);
  const logout = () => (user.value = null);
  return { user, setUser, logout };
}
```

💡 Совет:
• UI-компоненты — только отображение
• Composables/Stores — логика и состояние

O — Open/Closed Principle (OCP)

Суть:
Модуль должен быть открыт для расширения, но закрыт для изменения.

Пример на Vue/Nuxt:

```ts
// Базовый service для fetch
abstract class BaseService {
  abstract getData(): Promise<any>;
}

// Расширяем конкретным сервисом
class UserService extends BaseService {
  async getData() {
    return await $fetch('/api/users');
  }
}
```

• Можно создавать новые сервисы без изменения BaseService.
• В Vue/Nuxt также используется slots и composables, чтобы расширять функционал компонента, не меняя его код.

L — Liskov Substitution Principle (LSP)
Суть:
Подклассы должны заменять базовый класс без нарушения логики.

```ts
interface Logger {
  log(message: string): void;
}

class ConsoleLogger implements Logger {
  log(message: string) {
    console.log(message);
  }
}

class FileLogger implements Logger {
  log(message: string) {
    /* пишем в файл */
  }
}

function saveLog(logger: Logger) {
  logger.log('Saving log');
}

// Можно подставить любой Logger
saveLog(new ConsoleLogger());
saveLog(new FileLogger());
```

💡На фронтенде — применимо для сервисов API или store, когда нужно менять реализацию без изменения компонентов.

I — Interface Segregation Principle (ISP)

Суть:
Интерфейсы должны быть узкими и специфичными, а не перегружать функционал.

Пример:

```ts
interface Readable {
  read(): string;
}

interface Writable {
  write(data: string): void;
}

class File implements Readable, Writable {
  read() {
    return 'data';
  }
  write(data: string) {
    console.log(data);
  }
}
```

• В Vue/Nuxt: создаём маленькие интерфейсы для props, API, composables, чтобы компоненты не знали лишних методов.

D — Dependency Inversion Principle (DIP)

Суть:
Модули должны зависеть от абстракций, а не от конкретных реализаций.

```ts
interface IUserService {
  fetchUser(): Promise<User>;
}

class ApiUserService implements IUserService {
  async fetchUser() {
    return await $fetch('/api/user');
  }
}

function loadUser(service: IUserService) {
  return service.fetchUser();
}

// Можно подставить любую реализацию IUserService
loadUser(new ApiUserService());
```

• Во Vue/Nuxt: используем composables, store и provide/inject через интерфейсы, чтобы легко менять реализацию.
💡 Вывод для интервью:

Я всегда стараюсь применять SOLID при разработке фронтенда: UI-компоненты и composables разделены (SRP), расширяю функционал через composables/slots (OCP), использую интерфейсы и сервисы для LSP/DIP, а маленькие интерфейсы для props и API соблюдают ISP.

2️⃣ Архитектура фронтенда (Vue/Nuxt) — детальный разбор

1. Компонентная структура

Принцип: каждый компонент должен иметь чёткую ответственность и быть переиспользуемым.

Структура:

```
components/
  ui/           # базовые UI-компоненты (кнопки, инпуты, селекты)
  widgets/      # составные компоненты на базе UI (виджеты)
  modals/       # модальные окна
  layout/       # обертки страниц (default layout, 404)
  helpers/      # вспомогательные компоненты (обертки для fetch, спиннеры)
```

```vue
<!-- components/ui/Button.vue -->
<template>
  <button :class="type" @click="$emit('click')">{{ label }}</button>
</template>
<script setup lang="ts">
defineProps<{ label: string; type?: string }>();
defineEmits<['click']>();
</script>
```

🔹 UI-компоненты — только отображение
🔹 Widgets — собирают UI-компоненты вместе с логикой

2. Страницы и роутинг (Nuxt)
   • pages/ — автоматически создаёт маршруты
   • Динамические маршруты через [id].vue
   • Вложенные страницы через директории

Пример:

```
pages/
  index.vue       # главная
  posts/
    index.vue     # /posts
    [id]/index.vue # /posts/4
```

```vue
<script setup lang="ts">
import { useRoute } from 'vue-router';
const { params } = useRoute();
console.log(params.id);
</script>
```

3. Состояние приложения (Pinia)
   • Глобальное состояние для пользователя, корзины, настроек
   • Локальное состояние через ref и reactive
   • Строгая типизация через TypeScript

```ts
// stores/user.ts
import { defineStore } from 'pinia';

interface User {
  id: number;
  name: string;
}

export const useUserStore = defineStore('user', {
  state: () => ({ user: null as User | null }),
  actions: {
    setUser(u: User) {
      this.user = u;
    },
  },
});
```

4. Логика и переиспользуемые функции (Composables)
   • Переиспользуемая логика для API, состояния, форм
   • Отделяет бизнес-логику от UI
   • Чаще всего в composables/

```ts
// composables/usePosts.ts
import { ref } from 'vue';

export function usePosts() {
  const posts = ref([]);
  const isLoading = ref(false);

  async function fetchPosts() {
    isLoading.value = true;
    posts.value = await $fetch('/api/posts');
    isLoading.value = false;
  }

  return { posts, fetchPosts, isLoading };
}
```

5. API слой
   • Отдельная директория api или services
   • Все запросы централизованы
   • Обработка ошибок и состояния загрузки через async/await + try/catch

```ts
// api/user.ts
export async function getUser() {
  try {
    return await $fetch('/api/user');
  } catch (e) {
    console.error(e);
  }
}
```

6. Layouts
   • Обертки страниц для консистентного дизайна
   • Например: DefaultLayout.vue, AuthLayout.vue, ErrorLayout.vue

```vue
<template>
  <header>Header</header>
  <slot />
  <footer>Footer</footer>
</template>
```

7. Архитектурные принципы
   • Разделение ответственности (SRP из SOLID)
   • Повторное использование кода (UI → Widgets → Pages)
   • Централизованная логика (composables, store)
   • Чистая интеграция с API
   • Масштабируемость проекта — легко добавлять новые страницы и функционал

💡 Совет для интервью:

Опиши архитектуру через пример:
“UI-компоненты только отображают данные, widgets собирают их вместе, composables содержат логику, store хранит глобальное состояние, а API слой обрабатывает запросы. Это позволяет легко масштабировать проект и переиспользовать код.”

3️⃣ Структура данных и алгоритмы (Vue/Nuxt + TS)

1. Основные структуры данных

Массивы
• Используются для хранения списков элементов (posts, users, cart)
• Методы: map, filter, reduce, forEach
• Типизация через TypeScript:

```ts
interface User {
  id: number;
  name: string;
}
const users: User[] = [{ id: 1, name: 'Alice' }];
```

Объекты
• Хранят данные с ключами
• Удобно для быстрого доступа по ключу

```ts
const userMap: Record<number, User> = {};
userMap[1] = { id: 1, name: 'Alice' };
```

Map / Set
• Map — ключи любого типа, быстрый поиск, лучше чем объект для больших данных
• Set — уникальные значения

```ts
const idSet = new Set<number>();
idSet.add(1);
idSet.add(1); // не добавится

const userMap = new Map<number, User>();
userMap.set(1, { id: 1, name: 'Alice' });
console.log(userMap.get(1));
```

WeakMap / WeakSet
• Используются для слабых ссылок, чтобы объекты могли удаляться сборщиком мусора
• Применяются редко, чаще в сложных библиотеках

3. Примеры оптимизации в Vue/Nuxt
   • Кеширование данных в Pinia или composables:
   • Чтобы не дергать API повторно
   • Минимизация лишних рендеров:
   • v-if вместо скрытия элементов
   • v-for с key
   • Использование Map/Set для поиска и фильтрации:
   • Быстрее, чем array.find или filter на больших данных

“Я всегда стараюсь выбирать структуру данных под задачу: массивы для списков, Map/Set для быстрого поиска, объекты для хранения ключ-значение. Для оптимизации работы UI и API использую кеширование в Pinia, минимизирую лишние рендеры и применяю методы с линейной или константной сложностью.”

4️⃣ Паттерны проектирования во фронтенде (Vue/Nuxt + TS)

1. Singleton

Суть:
• Класс/объект имеет только один экземпляр
• Удобно для глобальных сервисов или хранилищ

Пример в Pinia:

```ts
// stores/user.ts
import { defineStore } from 'pinia';

export const useUserStore = defineStore('user', {
  state: () => ({ user: null }),
  actions: {
    setUser(u: any) {
      this.user = u;
    },
  },
});

// В любом компоненте — один и тот же store
const userStore = useUserStore();
```

🔹 Pinia сама реализует Singleton по сути: один store = один источник истины

2. Observer / Pub-Sub

Суть:
• Объект (Subject) уведомляет подписчиков (Observers) о событиях
• Во Vue — реактивность + события (emit, watch, ref)

Пример:

```ts
// composable useNotifier.ts
import { ref } from 'vue';

const messages = ref<string[]>([]);

export function useNotifier() {
  const notify = (msg: string) => messages.value.push(msg);
  return { messages, notify };
}

// В компонентах можно подписываться на messages
```

🔹 Используется для уведомлений, модалок, глобальных событий

3. Factory

Суть:
• Функция/класс создаёт объекты определённого типа
• Удобно для создания разных компонентов или моделей динамически

```ts
interface ButtonProps {
  label: string;
  type: string;
}

function createButton(props: ButtonProps) {
  return `<button class="${props.type}">${props.label}</button>`;
}

const primaryBtn = createButton({ label: 'Save', type: 'primary' });
```

🔹 Во Vue можно использовать для динамического создания компонентов через h()

4. Strategy

Суть:
• Определяет семейство алгоритмов и выбирает один из них в зависимости от ситуации
• Удобно для работы с разными API или валидацией

```ts
interface PaymentStrategy {
  pay(amount: number): void;
}

class CreditCardPayment implements PaymentStrategy {
  pay(amount: number) {
    console.log(`Paid ${amount} by card`);
  }
}

class PayPalPayment implements PaymentStrategy {
  pay(amount: number) {
    console.log(`Paid ${amount} by PayPal`);
  }
}

function checkout(strategy: PaymentStrategy, amount: number) {
  strategy.pay(amount);
}

checkout(new CreditCardPayment(), 100);
checkout(new PayPalPayment(), 100);
```

🔹 Во Vue/Nuxt можно применять для динамических методов работы с API, разных способов фильтрации или логики форм

Я использую Singleton для Pinia store, Observer через composables и watch, Factory для динамических компонентов, Strategy для переключения логики API или форм. Это делает код масштабируемым и легко поддерживаемым.

5️⃣ Алгоритмы и сложность (O-нотация) для фронтенда

1. O(1) — константная сложность

Суть:
• Время выполнения не зависит от размера данных
• Примеры во фронтенде:
• Доступ к элементу массива по индексу
• Чтение свойства объекта

```ts
const users = [{ id: 1, name: 'Alice' }];
console.log(users[0].name); // O(1)

const userMap = new Map<number, string>();
userMap.set(1, 'Alice');
console.log(userMap.get(1)); // O(1)
```

2. O(n) — линейная сложность

Суть:
• Время выполнения растёт пропорционально количеству элементов
• Примеры:
• forEach, map, filter
• Перебор массива для поиска элемента

```ts
users.forEach((u) => console.log(u.name)); // O(n)
const filtered = users.filter((u) => u.name === 'Alice'); // O(n)
```

💡 В Vue — каждый v-for создаёт O(n) рендеринг

3. O(n²) — квадратичная сложность

Суть:
• Вложенные циклы увеличивают время выполнения квадратично
• Пример:

```ts
users.forEach((u1) => users.forEach((u2) => console.log(u1.name, u2.name))); // O(n^2)
```

💡 Во Vue нужно избегать вложенных v-for, если массивы большие

4. O(log n) — логарифмическая сложность

Суть:
• Время выполнения растёт медленнее, чем размер данных
• Применяется для бинарного поиска, сортировок на больших массивах

```ts
function binarySearch(arr: number[], target: number): number {
  let left = 0,
    right = arr.length - 1;
  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (arr[mid] === target) return mid;
    arr[mid] < target ? (left = mid + 1) : (right = mid - 1);
  }
  return -1;
}
```

5. Практическое применение во Vue/Nuxt
   • Кеширование данных в Pinia → уменьшение O(n) запросов
   • Map/Set вместо filter/find → O(1) доступ вместо O(n)
   • Оптимизация рендеринга v-for → добавление key и lazy loading
   • Минимизация лишних вычислений → computed и watch

<!-- ЗАДАЧИ -->

Задача 1 — фильтрация пользователей

Условие:
Напиши функцию filterAdults, которая: 1. Принимает массив пользователей типа { id: number; name: string; age: number }[] 2. Возвращает массив только с пользователями старше 18 лет 3. Типизируй входные данные и возвращаемое значение на TypeScript

```ts
// My
interface Item {
  id: number;
  name: string;
  age: number;
}

function filterAdults(arr: Item[]): Item[] {
  return arr.filter((item) => item.age > 18);
}

filterAdults();
```

Похоже, в твоём коде есть несколько ошибок и недочётов, которые нужно исправить: 1. Синтаксис объявления функции — вместо functions должно быть function. 2. Тип возвращаемого значения — в TS хорошо сразу типизировать возвращаемое значение. 3. Вызов функции — нужно передать массив Item[], иначе TS/JS выдаст ошибку.

```ts
interface Item {
  id: number;
  name: string;
  age: number;
}

// функция с типизацией входного массива и возвращаемого значения
function filterAdults(arr: Item[]): Item[] {
  return arr.filter((item) => item.age > 18);
}

// пример данных
const users: Item[] = [
  { id: 1, name: 'Alice', age: 17 },
  { id: 2, name: 'Bob', age: 20 },
  { id: 3, name: 'Charlie', age: 25 },
];

// вызов функции
const adults = filterAdults(users);
console.log(adults);
// Вывод: [{ id: 2, name: 'Bob', age: 20 }, { id: 3, name: 'Charlie', age: 25 }]
```

✅ Исправления:
• functions → function
• Добавлен тип возвращаемого значения Item[]
• Функция вызывается с массивом аргументов

Задача 2 — исправь баги

Ниже пример кода с несколько ошибок и недочётов. Твоя задача — исправить их и сделать рабочий TypeScript код.

❌ Что нужно исправить: 1. Код возвращает массив с undefined для пользователей младше 18. 2. Нужно, чтобы массив содержал только пользователей старше 18. 3. Сохрани типизацию.

```ts
// код который нужно поправить
interface User {
  id: number;
  name: string;
  age: number;
}

const users: User[] = [
  { id: 1, name: 'Alice', age: 17 },
  { id: 2, name: 'Bob', age: 20 },
  { id: 3, name: 'Charlie', age: 25 },
];

const adultUsers = users.map((user) => {
  if (user.age > 18) {
    return user.name;
  }
});

console.log(adultUsers);
```

```ts
interface User {
  id: number;
  name: string;
  age: number;
}

const users: User[] = [
  { id: 1, name: 'Alice', age: 17 },
  { id: 2, name: 'Bob', age: 20 },
  { id: 3, name: 'Charlie', age: 25 },
];

const adultUsers = users.filter((user) => user.age > 18);

console.log(adultUsers);
```

все верно

```vue
<template>
  <div @click="selectUser(user.id)">
    <h3>{{ props.user.name }}</h3>
    <p>{{ props.user.age }}</p>
  </div>
</template>

<script lang="ts" setup>
interface User {
  id: number;
  name: string;
  age: number;
}

const props = defineProps<{ user: User }>();

const emit = defineEmits<{
  (e: 'selected', id: number): void;
}>();

function selectUser(id: number) {
  emit('selected', props.user.id);
}
</script>
```

```ts
const items = [
  { id: 1, name: 'Alice' },
  { id: 2, name: 'Bob' },
  { id: 1, name: 'Alice' }, // Дубликат
  { id: 3, name: 'Charlie' },
];

const uniqueItems = items.filter(
  (item, index, self) => self.findIndex((t) => t.id === item.id) === index,
);

console.log(uniqueItems);
// Результат: [{ id: 1, name: 'Alice' }, { id: 2, name: 'Bob' }, { id: 3, name: 'Charlie' }]

const uniqueMap = [...new Map(items.map((item) => [item.id, item])).values()];

// связанно с удалением дублей
```


SSR в Nuxt (Server-Side Rendering) — это режим, когда HTML страницы собирается на сервере на каждый запрос, а в браузере потом “оживает” через гидратацию.

Что происходит по шагам
	1.	Пользователь открывает URL
Браузер делает запрос на сервер (Nuxt сервер / Nitro).
	2.	Nuxt на сервере определяет маршрут
Находит страницу (pages/...) и её layout, плагины, middleware и т.д.
	3.	Сервер выполняет код страницы
	•	запускает нужные хуки/компосаблы для данных (например useAsyncData, useFetch)
	•	может сходить в API/БД
	•	рендерит Vue-компоненты в готовый HTML
	4.	Сервер отправляет ответ
В ответе обычно есть:
	•	готовый HTML (чтобы пользователь сразу увидел контент)
	•	подключенные JS/CSS
	•	“payload” с данными, которые уже были получены на сервере (чтобы не запрашивать их повторно)
	5.	Браузер делает гидратацию (hydration)
JS-бандл загружается, Vue “подхватывает” уже готовую разметку и привязывает:
	•	обработчики событий
	•	реактивность
	•	клиентскую навигацию (SPA-ощущение)
	6.	Дальше переходы по сайту чаще как SPA
После первой загрузки многие переходы происходят без полного перерендеринга страницы на сервере (зависит от настроек/данных), но SSR всё равно используется для прямых заходов и обновлений.

Где чаще всего “проявляется” SSR в Nuxt 3
	•	useAsyncData() / useFetch() — по умолчанию выполняются на сервере при SSR, а результат “приезжает” в payload.
	•	Серверные куски кода:
	•	server/api/* (ендпоинты Nitro)
	•	server/plugins/*
	•	runtimeConfig (секреты только на сервере)
	•	Важно помнить: на сервере нет DOM → нельзя напрямую использовать window, document без проверки.

```ts
if (process.client) {
  // код только для браузера
}
if (process.server) {
  // код только для сервера
}
```


вопрос по   http  что делает браузер что бы отдать нам страницу и отрисовать ее

Как браузер получает и отрисовывает страницу (HTTP → пиксели)

1) Запрос страницы
	1.	Пользователь вводит URL / кликает ссылку
	2.	DNS: домен → IP-адрес сервера
	3.	Соединение:
	•	TCP handshake
	•	если HTTPS → TLS handshake (шифрование)
	4.	Браузер отправляет HTTP-запрос (обычно GET /...)
	5.	Сервер обрабатывает и возвращает HTTP-ответ (HTML + заголовки)

⸻

2) Рендеринг (отрисовка)
	6.	Браузер парсит HTML → строит DOM
	7.	Находит ресурсы и догружает их:
	•	CSS (<link>)
	•	JS (<script>)
	•	изображения (<img>)
	•	шрифты и т.д.
	8.	Парсит CSS → строит CSSOM
	9.	Объединяет DOM + CSSOM → Render Tree (только видимые элементы)
	10.	Layout (Reflow): расчёт размеров и позиций элементов
	11.	Paint: рисование (текст, цвета, тени, картинки)
	12.	Composite: склейка слоёв (часто на GPU) → вывод на экран

⸻

3) Роль JavaScript
	13.	Выполняется JS:

	•	может изменить DOM/CSS, сделать запросы, повесить события
	•	изменения могут снова запускать:
Layout → Paint → Composite

⸻

Вся цепочка одной строкой

URL → DNS → TCP/TLS → HTTP request → HTML response → DOM → CSSOM → Render Tree → Layout → Paint → Composite